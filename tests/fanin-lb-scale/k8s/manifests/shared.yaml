apiVersion: v1
kind: Pod
metadata:
  name: shared
  labels:
    app: shared
spec:
  containers:
  - name: app
    image: fanin-lb-scale-shared:latest
    resources:
      requests:
        cpu: "10"
      limits:
        cpu: "10"
    env:
    - name: GOMAXPROCS
      value: "10"
    envFrom:
    - configMapRef:
        name: fanin-lb-scale-config
    ports:
    - containerPort: 2000
---
apiVersion: v1
kind: Service
metadata:
  name: fanin-lb-scale-shared
  labels:
    app: shared
spec:
  selector:
    app: shared
  ports:
  - name: http
    port: 2000
    targetPort: 2000
    protocol: TCP
