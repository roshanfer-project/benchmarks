apiVersion: v1
kind: Pod
metadata:
  name: backend1
  labels:
    app: backend1
spec:
  containers:
  - name: app
    image: multi-api-backend1:latest
    resources:
      requests:
        cpu: "2"
      limits:
        cpu: "2"
    env:
    - name: GOMAXPROCS
      value: "2"
    envFrom:
    - configMapRef:
        name: multi-api-config
    ports:
    - containerPort: 2000
  - name: envoy
    image: envoyproxy/envoy:v1.32-latest
    args:
    - "-c"
    - "/etc/envoy/envoy.yaml"
    - "--log-level"
    - "warn"
    - "--concurrency"
    - "1"
    - "--disable-hot-restart"
    resources:
      requests:
        cpu: "1"
      limits:
        cpu: "1"
    volumeMounts:
    - name: config-volume
      mountPath: /etc/envoy/envoy.yaml
      subPath: backend1.yaml
    ports:
    - containerPort: 3000
    - containerPort: 4000
    - containerPort: 9901
  - name: envoy-stats
    image: envoy-stats-exporter:latest
    env:
    - name: APP_NAME
      valueFrom:
        fieldRef:
          fieldPath: metadata.labels['app']
    - name: POLL_INTERVAL_MS
      value: "200"
    - name: OUTPUT_PATH
      value: "/tmp/envoy_stats.csv"
    resources:
      requests:
        memory: 16Mi
        cpu: 10m
      limits:
        memory: 32Mi
        cpu: 50m
  volumes:
  - name: config-volume
    configMap:
      name: envoy-configs
---
apiVersion: v1
kind: Service
metadata:
  name: multi-api-backend1
  labels:
    app: backend1
spec:
  selector:
    app: backend1
  ports:
  - name: envoy-ingress-3000
    port: 3000
    targetPort: 3000
    protocol: TCP
  - name: envoy-egress
    port: 4000
    targetPort: 4000
    protocol: TCP
