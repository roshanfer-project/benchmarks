syntax = "proto3";

package benchmark;

option go_package = "chain2lb/protobuf";

message Request { string payload = 1; }
message Response { string payload = 1; }

service backend {
  rpc f2(Request) returns (Response);
}

service frontend {
  rpc f1(Request) returns (Response);
}

