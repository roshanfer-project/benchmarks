---
apiVersion: v1
kind: Service
metadata:
  name: intermediate-diverse-entry
  labels:
    app: frontend
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
  - name: http
    port: 2000
    targetPort: 2000
    nodePort: 3000
    protocol: TCP
