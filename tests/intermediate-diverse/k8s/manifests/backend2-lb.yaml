apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend2
  labels:
    app: backend2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend2
  template:
    metadata:
      labels:
        app: backend2
    spec:
      containers:
      - name: app
        image: intermediate-diverse-backend2:latest
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "2"
          limits:
            cpu: "2"
        env:
        - name: GOMAXPROCS
          value: "2"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        envFrom:
        - configMapRef:
            name: intermediate-diverse-config
        ports:
        - containerPort: 2000
---
apiVersion: v1
kind: Service
metadata:
  name: intermediate-diverse-backend2
  labels:
    app: backend2
spec:
  clusterIP: None
  selector:
    app: backend2
  ports:
  - name: http
    port: 2000
    targetPort: 2000
    protocol: TCP
