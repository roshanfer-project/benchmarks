apiVersion: v1
kind: Pod
metadata:
  name: backend3
  labels:
    app: backend3
spec:
  containers:
  - name: app
    image: chain4-lb-backend3:latest
    resources:
      requests:
        cpu: "3"
      limits:
        cpu: "3"
    env:
    - name: GOMAXPROCS
      value: "3"
    envFrom:
    - configMapRef:
        name: chain4-lb-config
    ports:
    - containerPort: 2000
---
apiVersion: v1
kind: Service
metadata:
  name: chain4-lb-backend3
  labels:
    app: backend3
spec:
  selector:
    app: backend3
  ports:
  - name: http
    port: 2000
    targetPort: 2000
    protocol: TCP
