apiVersion: v1
kind: Pod
metadata:
  name: backend5
  labels:
    app: backend5
spec:
  containers:
  - name: app
    image: prob-route-fanout-backend5:latest
    resources:
      requests:
        cpu: "1"
      limits:
        cpu: "1"
    env:
    - name: GOMAXPROCS
      value: "1"
    envFrom:
    - configMapRef:
        name: prob-route-fanout-config
    ports:
    - containerPort: 2000
---
apiVersion: v1
kind: Service
metadata:
  name: prob-route-fanout-backend5
  labels:
    app: backend5
spec:
  selector:
    app: backend5
  ports:
  - name: http
    port: 2000
    targetPort: 2000
    protocol: TCP
