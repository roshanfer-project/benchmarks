apiVersion: v1
kind: Pod
metadata:
  name: backend3
  labels:
    app: backend3
spec:
  containers:
  - name: app
    image: prob-route-fanout-backend3:latest
    resources:
      requests:
        cpu: "2"
      limits:
        cpu: "2"
    env:
    - name: GOMAXPROCS
      value: "2"
    envFrom:
    - configMapRef:
        name: prob-route-fanout-config
    ports:
    - containerPort: 2000
---
apiVersion: v1
kind: Service
metadata:
  name: prob-route-fanout-backend3
  labels:
    app: backend3
spec:
  selector:
    app: backend3
  ports:
  - name: http
    port: 2000
    targetPort: 2000
    protocol: TCP
