apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend1
  labels:
    app: backend1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend1
  template:
    metadata:
      labels:
        app: backend1
    spec:
      containers:
      - name: app
        image: pfanout-2-backend1:latest
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "2"
          limits:
            cpu: "2"
        env:
        - name: GOMAXPROCS
          value: "2"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        envFrom:
        - configMapRef:
            name: pfanout-2-config
        ports:
        - containerPort: 2000
---
apiVersion: v1
kind: Service
metadata:
  name: pfanout-2-backend1
  labels:
    app: backend1
spec:
  clusterIP: None
  selector:
    app: backend1
  ports:
  - name: http
    port: 2000
    targetPort: 2000
    protocol: TCP
