apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend3
  labels:
    app: backend3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend3
  template:
    metadata:
      labels:
        app: backend3
    spec:
      containers:
      - name: app
        image: fan-out-3-backend3:latest
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "4"
          limits:
            cpu: "4"
        env:
        - name: GOMAXPROCS
          value: "4"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        envFrom:
        - configMapRef:
            name: fan-out-3-config
        ports:
        - containerPort: 2000
      - name: sidecar
        image: sidecar-sidecar:latest
        imagePullPolicy: Always
        env:
        - name: PROC_NAME
          value: "backend3-sidecar"
        - name: GLOG_logtostderr
          value: "1"
        resources:
          requests:
            cpu: "1"
          limits:
            cpu: "1"
        volumeMounts:
        - name: config-volume
          mountPath: /config.yaml
          subPath: backend3.yaml
        ports:
        - containerPort: 3000
        - containerPort: 4000
        - containerPort: 3000
          protocol: UDP
        - containerPort: 4000
          protocol: UDP
      volumes:
      - name: config-volume
        configMap:
          name: amphiqueue-configs
---
apiVersion: v1
kind: Service
metadata:
  name: fan-out-3-backend3
  labels:
    app: backend3
spec:
  clusterIP: None
  selector:
    app: backend3
  ports:
  - name: sidecar-ingress-tcp
    port: 3000
    targetPort: 3000
    protocol: TCP
  - name: sidecar-ingress-udp
    port: 3000
    targetPort: 3000
    protocol: UDP
  - name: sidecar-egress-tcp
    port: 4000
    targetPort: 4000
    protocol: TCP
  - name: sidecar-egress-udp
    port: 4000
    targetPort: 4000
    protocol: UDP
